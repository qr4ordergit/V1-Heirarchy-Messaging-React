import { Paper } from "@mantine/core";

export default function ChatPanel() {
  return (
    <Paper
      withBorder
      radius="md"
      className="h-full"
    >
      Chat Section
    </Paper>
  );
}